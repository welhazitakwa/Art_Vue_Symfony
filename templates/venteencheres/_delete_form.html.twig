<form method="post" action="{{ path('app_venteencheres_delete', {'id': venteenchere.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ venteenchere.id) }}">
    <button class="btn">Delete</button>
</form>
