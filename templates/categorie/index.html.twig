{% extends 'base.html.twig' %}

{% block title %}Categorie index{% endblock %}

{% block body %}
    <h1>Categorie index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Idcategorie</th>
                <th>Nomcategorie</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for categorie in categories %}
            <tr>
                <td>{{ categorie.idcategorie }}</td>
                <td>{{ categorie.nomcategorie }}</td>
                <td>
    <button onclick="window.location='{{ path('app_categorie_show', {'idcategorie': categorie.idcategorie}) }}'" class="btn btn-primary">Show</button>
    <button onclick="window.location='{{ path('app_categorie_edit', {'idcategorie': categorie.idcategorie}) }}'" class="btn btn-primary">Edit</button>
    <form method="post" action="{{ path('app_categorie_delete', {'idcategorie': categorie.idcategorie}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette catégorie ?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie.idcategorie) }}">
        <button class="btn btn-danger">Delete</button>
    </form>
</td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_categorie_new') }}">Create new</a>
    <h1>Nombre de catégories</h1>
    <p>Il y a {{ categoriesCount }} catégories.</p>
{% endblock %}
