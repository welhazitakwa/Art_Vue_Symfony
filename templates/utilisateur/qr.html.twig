{# templates/utilisateur/qr.html.twig

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>QR Codes</title>
</head>
<body>
    <h1>QR Codes</h1>

    <h2>QR Code avec logo de l'utilisateur :</h2>
    <img src="{{ img }}" alt="QR Code avec logo de l'utilisateur">

    <h2>QR Code simple :</h2>
    <img src="{{ simple }}" alt="QR Code simple">

    <h2>QR Code avec changement de couleur :</h2>
    <img src="{{ changeColor }}" alt="QR Code avec changement de couleur">

    <h2>QR Code avec changement de couleur d'arrière-plan :</h2>
    <img src="{{ changeBgColor }}" alt="QR Code avec changement de couleur d'arrière-plan">

    <h2>QR Code avec image :</h2>
    <img src="{{ withImage }}" alt="QR Code avec image">
</body>
</html> #}
{# templates/utilisateur/qr_result.html.twig #}

<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Résultat du QR Code</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            padding: 20px;
        }
        .qr-result {
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
            padding: 20px;
            max-width: 600px;
            margin: 0 auto;
        }
        .user-info {
            margin-bottom: 20px;
        }
        .user-info strong {
            font-weight: bold;
            margin-right: 10px;
        }
        .qr-code {
            margin-top: 20px;
            text-align: center;
        }
        .qr-code img {
            max-width: 100%;
        }
    </style>
</head>
<body>
    <div class="qr-result">
      <center>  <h1 style="color:#d5989e;">Résultat du QR Code</h1> </center> <br>
      	{% if app.session.get('user_profil') == 0 %}
           <center>  <a   href="{{path('app_home')}}" 
 style="color:#d5989e;font-size:20px; margin-left:400px">HOME </a> </center> 
       {% elseif app.session.get('user_profil') == 1 %} 
           <center>  <a             href="{{path('app_homeArtiste')}}" 
 style="color:#d5989e;font-size:20px; margin-left:400px">HOME </a> </center> 
       {% else %}
          <center><a href="{{path('app_home_client')}}" 
style="color:#d5989e;font-size:20px; margin-left:400px">HOME </a> </center> 
                                              {% endif %} 

        

        <div class="qr-code">
            
            <br>
            <br>
     <img src="{{ simple }}" alt="QR Code simple">
        </div>
    </div>
</body>
</html>
