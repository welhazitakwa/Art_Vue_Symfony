{% extends 'base.html.twig' %}

{% block title %}OeuvreConcours{% endblock %}

{% block body %}
    <h1>OeuvreConcours</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ oeuvre_concour.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_oeuvre_concours_index') }}">back to list</a>

    <a href="{{ path('app_oeuvre_concours_edit', {'id': oeuvre_concour.id}) }}">edit</a>

    {{ include('oeuvre_concours/_delete_form.html.twig') }}
{% endblock %}
