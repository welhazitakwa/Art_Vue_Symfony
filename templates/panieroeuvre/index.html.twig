{% extends 'base.html.twig' %}

{% block title %}Panieroeuvre index{% endblock %}

{% block body %}
    <h1>Panieroeuvre index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Quantite</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for panieroeuvre in panieroeuvres %}
            <tr>
                <td>{{ panieroeuvre.id }}</td>
                <td>{{ panieroeuvre.quantite }}</td>
                <td>
                    <a href="{{ path('app_panieroeuvre_show', {'id': panieroeuvre.id}) }}">show</a>
                    <a href="{{ path('app_panieroeuvre_edit', {'id': panieroeuvre.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_panieroeuvre_new') }}">Create new</a>
{% endblock %}
