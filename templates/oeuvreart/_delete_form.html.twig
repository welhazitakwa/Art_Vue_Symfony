<form method="post" action="{{ path('app_oeuvreart_delete', {'idoeuvreart': oeuvreart.idoeuvreart}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ oeuvreart.idoeuvreart) }}">
    <button class="btn">Delete</button>
</form>
